/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/traits]
            Element
        [Element/name]
            notification
        [Element/model]
            NotificationPopoverComponent:notification
`;

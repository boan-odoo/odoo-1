/** @odoo-module **/

import { registry } from '@web/core/registry';

registry.category('mailModelDefinitions').category('modelNamesToFetch').add('slide.channel');
registry.category('mailModelDefinitions').category('modelNamesToFetch').add('note.note');

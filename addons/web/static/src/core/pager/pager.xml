<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.Pager" owl="1">
        <nav class="o_pager d-flex align-items-center" aria-label="Pager">
            <span class="o_pager_counter mr-2" t-on-click.stop="">
                <t t-if="state.isEditing">
                    <input type="text" class="o_pager_value o_input text-right d-inline-block w-auto" size="7" t-ref="autofocus" t-att-value="value" t-on-blur="onInputBlur" t-on-change="onInputChange" t-on-keydown.stop="onInputKeydown" />
                </t>
                <t t-else="">
                    <span class="o_pager_value d-inline-block border-bottom border-white" t-esc="value" t-on-click="onValueClick" />
                </t>
                <span> / </span>
                <span class="o_pager_limit" t-esc="props.total" />
            </span>
            <span class="btn-group" aria-atomic="true">
                <!-- accesskeys not wanted in X2Many widgets -->
                <button type="button" class="oi oi-chevron-left--thick btn btn-secondary o_pager_previous px-2 rounded-left" aria-label="Previous" data-tooltip="Previous" tabindex="-1" t-att-data-hotkey="props.withAccessKey ? 'p' : false" t-att-disabled="state.isDisabled or isSinglePage" t-on-click.stop="() => this.navigate(-1)" />
                <button type="button" class="oi oi-chevron-right--thick btn btn-secondary o_pager_next px-2 rounded-right" aria-label="Next" data-tooltip="Next" tabindex="-1" t-att-data-hotkey="props.withAccessKey ? 'n' : false" t-att-disabled="state.isDisabled or isSinglePage" t-on-click.stop="() => this.navigate(1)" />
            </span>
        </nav>
    </t>

</templates>
